/* eslint-disable */
import modelObject from '@/services/lb-services';

modelObject.setModel('Payments');

let payments = Object.assign({}, {

    relation: ''

}, modelObject);

export default payments;
