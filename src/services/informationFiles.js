/* eslint-disable */
import modelObject from '@/services/lb-services';
import loopback from '@/services/loopback';

modelObject.setModel('InformationFiles');

let informationFiles = Object.assign({}, {

    relation: '',

}, modelObject);

export default informationFiles;
